import type { NextConfig } from "next";

// Production canonical domain is https://www.myhealthdata.foundation.
// Non-www → www permanent redirect is configured in vercel.json (Vercel).
const nextConfig: NextConfig = {
  /* config options here */
};

export default nextConfig;
