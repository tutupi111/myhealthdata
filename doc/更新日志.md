# 更新日志

本文档记录 myhealthdata 项目的版本变更，按时间倒序排列。每次重要改动请在此追加条目，并同步更新相关说明文档。

**文档最后更新**：2026-05-21 23:10:06（UTC+8）

---

## v0.2.0 — 2026-05-21

当前生产可用版本，包含联系表单、Data Hub 对接与首页视觉升级。

### 2026-05-21 23:07:57 · 首页视觉升级

**Commit**：`9381763` — Redesign homepage with HealthData dark theme and updated layout

**变更摘要**：

- 首页全面改版，参考 HealthData 深色科技风设计
- 品牌展示统一为 **HealthData**（导航 / 页脚 Logo）
- 新 Hero：标签徽章、左右分栏、3D 层叠 SVG 插画（`HeroIllustration`）
- 导航栏改为深色 sticky，菜单项：Home / Platform / Use Cases / Docs / About / Open Source
- 右侧新增「Join the Movement」按钮；语言切换支持 dark 样式
- 页脚改为四列：品牌、Platform、Resources、About
- 首页区块文案与统计数字更新（如 100K+ Users、30+ Health Partners）
- 平台入口卡片：个人 / 研究者 / 组织机构；组织入口 CTA 指向联系页
- 新增组件：`HeroIllustration.tsx`、`SectionHeader.tsx`（含 `ProblemCard`）
- 更新组件：`HeroSection`、`Navbar`、`Footer`、`FeatureCard`、`PortalCard`、`ProjectCard`、`GlobalImpactCard`
- 设计 token 更新：`globals.css` 新增 `#0a0e14`、`#00d1b2` 等变量与动画类

**涉及文件**（15 个）：

- `app/[lang]/page.tsx`
- `app/[lang]/projects/page.tsx`
- `app/globals.css`
- `components/HeroIllustration.tsx`（新增）
- `components/SectionHeader.tsx`（新增）
- `components/HeroSection.tsx`
- `components/Navbar.tsx`
- `components/Footer.tsx`
- `components/FeatureCard.tsx`
- `components/PortalCard.tsx`
- `components/ProjectCard.tsx`
- `components/GlobalImpactCard.tsx`
- `components/LanguageSwitcher.tsx`
- `components/icons.tsx`
- `lib/i18n/translations.ts`

**不影响**：Vercel 环境变量（`DATA_HUB_*`）及联系表单 API 逻辑。

---

### 2026-05-21 22:57:13 · 联系表单错误处理优化

**Commit**：`fdf46da` — Improve contact form error handling and Data Hub env compatibility

**变更摘要**：

- API 路由改为运行时读取环境变量，支持 `VITE_DATA_HUB_*` 别名
- 503（未配置凭据）与通用错误分开提示
- 上游 Data Hub 失败时增加服务端日志
- 更新 `.env.example`，注明 Vercel 需勾选 Production 并 Redeploy

**涉及文件**：

- `app/api/contact-events/route.ts`
- `lib/contactTracking.ts`
- `components/ContactForm.tsx`
- `lib/i18n/translations.ts`
- `.env.example`

---

### 2026-05-21 22:32:31 · 联系页与 Data Hub 对接

**Commit**：`1b3b567` — Add contact form with Data Hub integration and project docs

**变更摘要**：

- 联系页完善：展示邮箱 `hugchen1117@gmail.com` + 留言表单
- 新增服务端代理 `POST /api/contact-events`，对接 App Data Hub
- 新增前端埋点 `lib/contactTracking.ts`
- 页脚 / 联系页邮箱点击上报 `channel: email`
- 表单提交上报 `channel: form`，内容写入 `metadata`
- 新增 `doc/` 项目文档目录及对接指南副本
- 新增 `.env.example`

**涉及文件**（核心）：

- `app/api/contact-events/route.ts`（新增）
- `app/[lang]/contact/page.tsx`
- `components/ContactForm.tsx`（新增）
- `components/ContactEmailLink.tsx`（新增）
- `lib/contactTracking.ts`（新增）
- `lib/config.ts`（新增 `CONTACT_EMAIL`）
- `components/Footer.tsx`
- `doc/`（新增）

**部署要点**：

- Vercel 需配置 `DATA_HUB_BASE_URL`、`DATA_HUB_APP_ID`、`DATA_HUB_API_KEY`
- 配置后必须 **Redeploy**，否则接口返回 503

---

## v0.1.0 — 2026-03-12 ~ 2026-03-14

初始官网版本。

### 2026-03-14 11:14:43 · App 登录联动

**Commit**：`a317bca` — Connect website login to app.myhealthdata.foundation

- Navbar 读取 `mhdf_logged_in` Cookie，显示登录 / 进入管理中心
- 门户页跳转 `app.myhealthdata.foundation/login?role=...`

### 2026-03-12 01:19:39 · 首页设计 v3

**Commit**：`123a1ca` — upgrade homepage design v3

- 多区块营销落地页、双语支持、SEO、响应式布局

---

## 下次修改时请更新

1. 在本文件顶部更新 **文档最后更新** 时间
2. 追加新的版本条目（日期、Commit、变更摘要、涉及文件）
3. 同步修改 [已完成功能](./已完成功能.md)、[页面与路由](./页面与路由.md)、[部署与配置](./部署与配置.md) 中过时的描述
4. 如有新版本号，更新 [README](./README.md) 与 [项目概述](./项目概述.md) 中的版本号
